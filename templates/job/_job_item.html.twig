<section class="text-slate-700 body-font overflow-hidden">
    <div class="container px-5 py-24 mx-auto">
        <div class="-my-8 divide-y-2 divide-gray-100">
            <div class="py-8 flex flex-wrap md:flex-nowrap">
                <div class="md:w-64 md:mb-0 mb-6 flex-shrink-0 flex flex-col">
                    <span class="text-slate-700 font-semibold title-font text-gray-700">Job Offer</span>
                    <span class="mt-1 text-slate-700 text-sm">{{ job.postDate|date('F j, Y') }}</span>
                </div>
                <div class="md:flex-grow">
                    <h2 class="text-slate-700 text-lg title-font font-medium mb-3">{{job.title}}</h2>
                    <p class=" text-slate-700 leading-relaxed">{{ job.description|slice(0, 100) }}...</p>
                    <a class="text-indigo-500 inline-flex items-center mt-4" href="{{path('app_job_show', {'id':job.id})}}">View details
                        <svg class="w-4 h-4 ml-2" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M5 12h14"></path>
                            <path d="M12 5l7 7-7 7"></path>
                        </svg>
                    </a>
                </div>
            </div>
            <div>